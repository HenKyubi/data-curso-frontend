/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  swcMinify: true,
  
  // basePath: "/data-curso",
  // assetPrefix: "/data-curso",
};

module.exports = nextConfig;
